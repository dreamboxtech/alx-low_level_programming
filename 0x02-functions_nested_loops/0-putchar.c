#include <stdio.h>
/**
 * main - does som main tasks
 * Return: Returns main's
 */
int main(void)

{  
	char* st = "_putchar\n";
        int i;

	for (i = 0; i < 9; i++)
	{
		putchar(st[i])
	}
	return (0);
{
