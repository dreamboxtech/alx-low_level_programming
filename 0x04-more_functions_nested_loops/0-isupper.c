#include <cytype.h>
#include "main.h"

/**
 * _isupper - Does the function of checking
 * @c: variable
 * Return: 1 or 0
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1)
	}
	else
	{
		return (0)
	}
}
